import React from 'react';

type ConsentChoice = 'ACCEPT_ALL' | 'REJECT_ALL' | 'CUSTOMIZE' | 'LEARN_MORE';

interface Props {
    onConsent: (choice: ConsentChoice) => void;
}
declare const CookieConsentDialog1: ({ onConsent }: Props) => React.JSX.Element | null;

interface CookieConsentDialogProps {
    onChoice: (choice: string[]) => void;
}
declare function CookieConsentDialog({ onChoice }: CookieConsentDialogProps): React.ReactPortal | null;

declare function useCookieConsent(): string[] | null;

declare function emitConsentUpdate(consent: string[]): void;

export { CookieConsentDialog, CookieConsentDialog1 as Dialog, emitConsentUpdate, useCookieConsent };
